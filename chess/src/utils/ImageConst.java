package utils;

/**
 * Constants related to image states
 */
public class ImageConst {
    // Image is not hovered over
    public static final int IMG_DOWN   = 0;
    // Hover over image
    public static final int IMG_UP     = 1;
    // Total image states
    public static final int IMG_STATES = 2;
}
